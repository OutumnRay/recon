# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
/tmp/

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work

# Build artifacts
cmd/managing-portal/dist/
cmd/user-portal/dist/

# Frontend artifacts are built during Docker build
# Keep the source in front/ but ignore built dist in cmd/
/jitsy/jitsi-recorder-lite/myenv/

# Android release keystore (local signing credentials)
mobile2/android/keystore.jks

.cache

# Auto-generated version files (generated by build.sh)
front/user-portal/src/config/version.ts
front/managing-portal/src/config/version.ts
internal/version/version.go

# Firebase credentials (NEVER commit these!)
recontext-firebase.json
*-firebase.json
!*-firebase.json.example
firebase-credentials.json
google-services.json
GoogleService-Info.plist