# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
/tmp/

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work

# Build artifacts
cmd/managing-portal/dist/
cmd/user-portal/dist/

# Frontend artifacts are built during Docker build
# Keep the source in front/ but ignore built dist in cmd/
